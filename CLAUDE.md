# Project Instructions

## Git
- When the user asks to commit, always commit immediately without asking for confirmation.
- Always use this exact two-step pattern for commits: `git add <files>` then `git commit -m "message"`. Keep commit messages to a single short line with no body to keep the command consistent.
